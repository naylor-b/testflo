testflo is a python testing framework that uses a flow oriented design.
Test path strings and later TestResult objects flow through a series
of objects that operate on them and pass them on to the next object
in the pipe.

